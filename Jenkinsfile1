pipeline {
    agent any
    
    environment {
        SLACK_CHANNEL = '#purple'
        DOCKER_IMAGE = 'yersmi13/mon-portfolio'
        DOCKER_TAG = "build-${env.BUILD_NUMBER}"
        DEPLOY_PORT = '8060'  
    }
    
    triggers {
        pollSCM('* * * * *')  // Vérification toutes les minutes
    }
    
    stages {
        stage('Détection Commit') {
            steps {
                script {
                    // Récupérer les infos du commit
                    def commitHash = sh(script: 'git rev-parse --short HEAD', returnStdout: true).trim()
                    def commitMessage = sh(script: 'git log -1 --pretty=%B', returnStdout: true).trim()
                    def author = sh(script: 'git log -1 --pretty=%an', returnStdout: true).trim()
                    
                    slackSend(
                        channel: "${env.SLACK_CHANNEL}",
                        color: "good",
                        message: """🚀 NOUVEAU COMMIT DÉTECTÉ - Build #${env.BUILD_NUMBER}
                        📝 Message: ${commitMessage}
                        👤 Auteur: ${author}
                        🔗 Commit: ${commitHash}
                        🌿 Branch: ${env.GIT_BRANCH}"""
                    )
                }
            }
        }
    
        stage('Clone') {
            steps {
                script {
                    slackSend channel: "${env.SLACK_CHANNEL}", 
                             color: 'good', 
                             message: "📥 Début du clonage - Build #${env.BUILD_NUMBER}"
                    
                    echo "Clonage du dépôt de la branche dev..."
                    // Le checkout est déjà fait automatiquement par Jenkins, on vérifie juste
                    sh 'git status'
                }
            }
            post {
                success {
                    script {
                        def commitInfo = sh(script: 'git log -1 --oneline', returnStdout: true).trim()
                        slackSend channel: "${env.SLACK_CHANNEL}", 
                                 color: 'good', 
                                 message: "✅ CLONE RÉUSSI\nRepository: mon-portfolio"
                    }
                }
                failure {
                    slackSend channel:"${env.SLACK_CHANNEL}", 
                             color: 'danger', 
                             message: "❌ ÉCHEC DU CLONE\nImpossible d'accéder au dépôt"
                }
            }
        }
        
        stage('Build') {
    steps {
        script {
            echo "🔨 Construction de l'image Docker du portfolio..."

            // Copie du Dockerfile depuis ton projet pour éviter les confusions
            sh 'cp Dockerfile.txt Dockerfile'

            // Construction de l’image sans utiliser de prune/rm agressif
            sh """
                docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} -f Dockerfile .
                docker tag ${DOCKER_IMAGE}:${DOCKER_TAG} ${DOCKER_IMAGE}:latest
            """
        }
    }
    post {
        success {
            script {
                def imageSize = sh(
                    script: "docker images ${DOCKER_IMAGE}:latest --format '{{.Size}}'",
                    returnStdout: true
                ).trim()

                slackSend channel: "${SLACK_CHANNEL}",
                    color: 'good',
                    message: "✅ BUILD RÉUSSI\nImage: ${DOCKER_IMAGE}:latest\nTaille: ${imageSize}"
            }
        }
        failure {
            slackSend channel: "${SLACK_CHANNEL}",
                color: 'danger',
                message: "❌ ÉCHEC DU BUILD\nErreur lors de la construction Docker"
        }
    }
        }

        stage('Push DockerHub') {
            steps {
                script {
            withCredentials([string(credentialsId: 'token-dockerhub', variable: 'DOCKER_TOKEN')]) {
                sh """
                    echo "$DOCKER_TOKEN" | docker login --username yersmi13 --password-stdin
                    docker push ${DOCKER_IMAGE}:${DOCKER_TAG}
                    docker push ${DOCKER_IMAGE}:latest
                """
                    }
                }
            }
            post {
                success {
                    slackSend channel: "${SLACK_CHANNEL}",
                        color: 'good',
                        message: "✅ IMAGE PUSHÉE SUR DOCKER HUB\nImage: ${DOCKER_IMAGE}:${DOCKER_TAG}\nLien: https://hub.docker.com/r/yeillenr/portfolio"
                }
                failure {
                    slackSend channel: "${SLACK_CHANNEL}",
                        color: 'danger',
                        message: "❌ ÉCHEC DU PUSH DOCKER HUB\nVérifie ton fichier secret ou ta connexion"
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    echo "🚀 Déploiement du portfolio..."
                    
                    // Arrêter et supprimer l'ancien conteneur s'il existe
                    sh '''
                        echo "Nettoyage des anciens conteneurs..."
                        docker stop portfolio-dev || true
                        docker rm portfolio-dev || true
                    '''
                    
                    // Lancer le nouveau conteneur
                    sh """
                        docker run -d \
                            --name portfolio-dev \
                            -p ${env.DEPLOY_PORT}:80 \
                            ${env.DOCKER_IMAGE}:${env.DOCKER_TAG}
                    """
                    
                    // Attendre que l'application soit prête
                    sleep 10
                    
                    // Test du déploiement - CORRECTION DU PORT
                    sh "curl -f http://localhost:${env.DEPLOY_PORT} || exit 1"
                    
                    echo "✅ Déploiement réussi sur le port ${env.DEPLOY_PORT}"
                }
            }
            post {
                success {
                    slackSend channel: "${env.SLACK_CHANNEL}", 
                             color: 'good', 
                             message: "🎉 DÉPLOIEMENT RÉUSSI\nPortfolio disponible: http://localhost:${env.DEPLOY_PORT}\nConteneur: portfolio-dev\nImage: ${env.DOCKER_IMAGE}:${env.DOCKER_TAG}"
                }
                failure {
                    slackSend channel: "${env.SLACK_CHANNEL}", 
                             color: 'danger', 
                             message: "❌ ÉCHEC DU DÉPLOIEMENT\nImpossible de déployer le portfolio"
                    
                    // Nettoyage en cas d'échec
                    sh '''
                        docker stop portfolio-dev || true
                        docker rm portfolio-dev || true
                    '''
                }
            }
        }
    }
    
    // POST pour l'ensemble du pipeline - CORRECTEMENT PLACÉ
    post {
        always {
            echo "🏁 Pipeline terminé - Résultat: ${currentBuild.currentResult}"
            script {
                // Nettoyage des images temporaires
                sh '''
                    echo "Nettoyage des images Docker..."
                    docker images -f "dangling=true" -q | xargs -r docker rmi || true
                '''
            }
        }
        success {
            slackSend channel: "${env.SLACK_CHANNEL}", 
                     color: 'good', 
                     message: "🎊 PIPELINE TERMINÉ - SUCCÈS\nBuild #${env.BUILD_NUMBER}\nDurée: ${currentBuild.durationString}\nDétails: ${env.BUILD_URL}"
        }
        failure {
            slackSend channel: "${env.SLACK_CHANNEL}", 
                     color: 'danger', 
                     message: "💥 PIPELINE TERMINÉ - ÉCHEC\nBuild #${env.BUILD_NUMBER}\nDurée: ${currentBuild.durationString}\nDétails: ${env.BUILD_URL}"
        }
        unstable {
            slackSend channel: "${env.SLACK_CHANNEL}", 
                     color: 'warning', 
                     message: "⚠️ PIPELINE TERMINÉ - INSTABLE\nBuild #${env.BUILD_NUMBER}\nDétails: ${env.BUILD_URL}"
        }
    }
}
